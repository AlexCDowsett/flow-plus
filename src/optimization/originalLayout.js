// Original Layout Method
// Returns data as-is without any optimization

/**
 * Original Layout Method - Returns data as-is (no optimization)
 * @param {Object} data - The flow data object
 * @returns {Object} - Original flow data unchanged
 */
export const originalLayout = (data) => {
  console.log('📋 Using original layout (no optimization)');
  return data;
};
